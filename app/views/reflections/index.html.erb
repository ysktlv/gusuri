<%= render "shared/header" %>
<div class="info">
  マイページ
</div>
<div class="reflections">
  <%= form_with model: @reflection, local: true do |f| %>
    <%= f.date_field :start_time %>
    <%= f.text_field :impression %>
    <%= f.collection_check_boxes :goal_ids, Goal.all, :id, :name %>
    <%= f.submit %>
  <% end %>
</div>
<%= month_calendar events: @reflections do |date, reflections| %>
  <%= date.day %>
  <% reflections.each do |reflection| %>
    <div>
      <%= link_to reflection.impression, reflection %>
    </div>
  <% end %>
<% end %>